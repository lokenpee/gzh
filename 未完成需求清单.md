# 未完成需求清单（原文）

1. 我要的默认展示逻辑没实现啊，如果关键词填空，那么就应该默认进入到默认模式，就是不调用llm进行语义，默认实体关键词为关税战和海湖庄园协议，然后的话也不向数据库发送请求，默认以项目文件夹中的关税战海湖庄园协议_完整字段.xlsx，作为数据，直接进行解析整理分析，进入到展示环节，你懂我意思吗，不懂的话来问我我们对齐颗粒度

2. 第一：文章数:'+p.data;
function(params)(var p=params&ms[o];if(lp)(return ";Jreturn p.axisValue +'，事件传播趋势有问题，每个点都显示这个没有数量这个图看起来就是乱码根本看不了，如图
第二,观点提炼有问题，现在是把top10的观点都提炼了，我需要的是聚类，把前十的观点中意见相同的聚成一类，最后可能会形成2-4个观点类型，每个的角度都不同，每个观点的格式是核心观点，观点详情（包括数据逻辑推演等有就写），来源（一个观点可以来源多个众号：文章，一个公众号：文章也能对应多个观点，每篇文章标记上时间），

3. 向数据库请求正文包含所有核心实体词的文章数据后，在进入分析文章各模块消费前，还需要经过文章正文与llm意图识别解析出的完整事件（event_name: "规范化事件名，不超过30字，包含核心主体+关键动作+时间/地点（如有）"）进行相关性分析，以保证该文章确实是讲这件事的，而不是只是恰好包括了这些关键词。我的想法大概是这些，你有什么想法吗，我的目的是除了关键词匹配以外还要经过一轮精筛，你有什么好的想法吗。你说的：方案 C：混合（推荐）关键词 AND → Embedding 粗筛 → LLM 精筛（只处理少量文章）。
这是性价比最高的方案，准确率也更好。。我说：我把方案 C：混合（推荐）

关键词 AND → Embedding 粗筛 → LLM 精筛（只处理少量文章）。
这是性价比最高的方案，准确率也更好。
写到prd了，你看看有什么问题，跟我对一下然后施工

我的emb模型

model：
Qwen/Qwen3-VL-Embedding-8B
Qwen3-VL-Embedding-8B（我不清楚叫哪个）

key：sk-anluvnuttmpxxpztodcgsdwsypqmleiyhojattjgckzsiofj

curl -X POST https://api.siliconflow.cn/v1/embeddings
-H "Authorization: Bearer $SILICONFLOW_API_KEY"
-H "Content-Type: application/json"
-d '{
"input": "Hello, world!",
"model": "Qwen/Qwen3-VL-Embedding-8B"
}'

4. 第二，事件传播趋势那块，我还希望用户鼠标悬浮到某天的文章数量上时，他还能展现一个文章列表框，展示了当天的该数量的文章列表，按照阅读量排序，分别是文章名称，作者，发布时间，阅读量

5. 还有一个问题是，我管机器输入了其他关键词，都不是默认情况，所任接通了llm语义理解和联网，但是并没有接通公司的数据库。但是数据仍然从那个模拟数据excel表中获取，模拟数据excel只是作为展示功能用的ok？还有ui上面仍有数据源是模拟数据excel这类的话，这是给客户看的，怎么能有这些呢，而且数据源也不是模拟数据excel啊完全隐藏数据源提示，而且还有一个问题是，我管机器输入了其他关键词，都不是默认情况，所任接通了llm语义理解和联网，但是并没有接通公司的数据库。但是数据仍然从那个模拟数据excel表中获取，模拟数据excel只是作为展示功能用的ok？改吧

6. 韬定律——华为韬定律何庭波这个关键词识别，如何决定输出这些的，全靠大模型吗，现在有个问题是，大模型输出的这三个核心实体，是用来从数据库中调取数据的的（必须完全包含这三个关键词）用户输入韬定律已经完全足够锁定事件了，加入华为这个关键词做and，无关紧要因为有韬定律必定会提到华为，但是再加入何庭波就有问题了，只会进一步缩小数据范围，可能漏掉关键信息，怎么办呢，我这一步目的很单纯，只想要筛掉无关事件而已，比如我说openclaw就不想要农业的文章，你有什么好的解决方法不要增加过多的复杂度，就用实体词来来缩小范围够了，不需要再分层，我现在是考虑如何让模型更精确的输出核心实体，不输出何庭波这种附属字段，这种只会缩减范围实体的个数不是关键，关键是如何让他懂得那个度，什么词能加什么不能。能否来一个值，如果这个关键词置信度低于这个值就不加入核心实体这类的。你说：A) 基于搜索证据的置信度（不依赖 DB，最简单）
对每个实体词 e，在联网搜索结果里算它“被反复提到”的程度（人物名往往只在少数结果里出现）：

title_hits = 有多少条 result.title 包含 e
snippet_hits = 有多少条 result.snippet 包含 e
置信度（0~1）：
[
score(e)=\frac{2\cdot title_hits + 1\cdot snippet_hits}{3\cdot N}
]
其中 N=搜索结果条数（比如你们 SEARCH_LIMIT=5）。我回答：跟你的方案a类似，但是改一下，比如说联网搜索到了number=十篇内容，然后ai输出了1-3个关键词，统计hits=有多少篇内容提到了关键词（是内容不是标题这得看api返回的格式是什么才好选那个字段，我的意思是标题不一定能包含所有信息，用全部内容就行）。计算如果这个实体关键词的hits/number大于80%，则可以纳入核心实体，不能就去掉，核心实体最后保留1-3个


1.运行日志在默认模式下仍打印 "检索服务: Tavily | 返回条数: 5"，但实际没有联网检索（这个不用啊，原本意思就是空着的话，就走默认，默认的话就不llm也不像数据库请求，但是也走emb和llm相关性）btw，你后面这个相关性是怎么实现的跟我讲一下
2.运行日志中 "本地演示模式" 含"演示"字样，没关系，只要逻辑实现就行，页面不展示就行

这个搜索api能否限制文章时间，比如说我选了2025-12-03 2026-04-16，他就只展示这个范围的文章，我现在用百度api的话，他似乎没有受到时间限制，展示的都是这几天的文章，内容当然都是农业的小龙虾

改一下一个地方，就是默认模式下，也就是关键词留空的时候，就是默认用户输入了关税战海湖庄园协议，还是要经过llm语义识别和联网的，跟正常的链路一样，只不过不经过向公司数据库发送请求，就当作项目根目录的那个关税战海湖庄园协议_完整字段.xlsx，就是公司返回的数据，就还是要经过核心实体关键词and链路匹配初筛和emb粗筛，以及llm精筛。然后正常的ui展示
关键词 AND 匹配 ❌ — Excel 里根本没有小龙虾的文章，filter_articles 返回空（不对有关键词的的时候不应该走模拟数据应该走sql请求公司数据库，因为我先没有配置所以才走不通你的理解错了）

（事件名称：海湖庄园协议引发全球热议
核心实体：
海湖庄园协议
关联标的：
未识别
关键节点：
2025-02-01 · 海湖庄园协议传言引起华尔街讨论2025-03-01 · 美国一季度GDP预测大幅下调至-2.8%2025-03-27 · 国泰海通证券发布相关研究报告）上面是llm联网检索的结果，我觉得大有问题，第一没有识别到关联标的？这显然没调用到llm。这一模块我的设想是用户输入关键词（关税战海湖庄园协议）——llm（或者硬编码）扩展解锁关键词（关税战 海湖庄园协议 事件 特朗普 2025/02/01 – 2025/03/31）——联网api检索——内容返回llm大模型——大模型理解事件整理出：事件名称：核心实体：关联标的：关键节点：。先不说原先的链路跟我的应该不一样，还有其他的问题：1.事件名称太简陋
2.居然没有关联标的llm得到了信息动动脑子呢（建议时那种金融终端能检索到的标的比如说股票/板块/大宗商品/期货这些走势）
3.而且关键时间点也牛头不对马嘴，后面两个事件跟关税战海湖庄园协议有半毛钱关系吗

（事件名称：海湖庄园协议引发全球热议
核心实体：
海湖庄园协议
关联标的：
未识别
关键节点：
2025-02-01 · 海湖庄园协议传言引起华尔街讨论2025-03-01 · 美国一季度GDP预测大幅下调至-2.8%2025-03-27 · 国泰海通证券发布相关研究报告）上面是llm联网检索的结果，我觉得大有问题，第一没有识别到关联标的？这显然没调用到llm。这一模块我的设想是用户输入关键词（关税战海湖庄园协议）——llm（或者硬编码）扩展解锁关键词（关税战 海湖庄园协议 事件 特朗普 2025/02/01 – 2025/03/31）——联网api检索——内容返回llm大模型——大模型理解事件整理出：事件名称：核心实体：关联标的：关键节点：。先不说原先的链路跟我的应该不一样，还有其他的问题：1.事件名称太简陋
2.居然没有关联标的llm得到了信息动动脑子呢（建议时那种金融终端能检索到的标的比如说股票/板块/大宗商品/期货这些走势）
3.而且关键时间点也牛头不对马嘴，后面两个事件跟关税战海湖庄园协议有半毛钱关系吗