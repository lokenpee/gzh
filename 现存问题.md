1.联网搜索不可用，我都给你配置了你还不会用吗curl --location 'https://api.qnaigc.com/v1/search/web' \
  --header 'Content-Type: application/json' \
  --header 'Authorization: Bearer <API_KEY>' \
  --data '{
    "query": "今日新闻",
    "max_results": 10,
    "search_type": "web",
    "time_filter": "year",
    "site_filter": ["news.cctv.com", "www.xinhuanet.com"]
}'

2.时间趋势坐标轴数据乱码：function(params)(var p=params&ms[o]if(lp)(return ";Jreturn p.axisValue +"文章数:'+p.data;)